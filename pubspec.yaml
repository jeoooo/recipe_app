name: recipe_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_summernote: ^1.1.1
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  html_editor_enhanced: ^2.5.1
  supabase_flutter: ^1.10.25

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/app_logo.svg
    - lib/assets/login_app_logo.svg
    - lib/assets/sign_up_logo.svg
